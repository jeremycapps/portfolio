# Archived golden-case docs

**Archived:** 2026-09-03. Superseded by `../golden-case-klarna-ai-support.md`.

Klarna is the canonical golden case now — one bounded decision, recent, and vindicated by
the protagonist rather than a too-big-to-fail postmortem that reads as a humiliation tool.
See [Klarna golden case](../golden-case-klarna-ai-support.md) and the
[crosswalk](../tempo-enterprise-crosswalk.md).

**These are docs only.** The corresponding **code cases stay live**:
`src/lib/stratos/cases/*.ts` (Target Canada, Watson/MD Anderson, VA EHR, McDonald's,
Adobe, Domino's, Ford) still power the flow view and its tests, and Klarna is not yet
implemented as a code case. Do not remove the code cases until Klarna replaces them; these
handoffs are kept for reference and provenance.
