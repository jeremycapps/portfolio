# Portfolio

A quiet, interactive AI assistant portfolio surface built with React, TypeScript, Vite, Tailwind CSS, and Radix UI.

## Run locally

```bash
pnpm install
pnpm run dev
```

The app is designed as a focused presentation workspace with an interactive prompt composer, connection states, file attachment affordance, and selected document work.
